version: '3'

services:
  redis:
    image: redis:7.4.1
    restart: always

  codebox:
    build: .
    cap_add:
      - NET_ADMIN
      - cap_net_admin
      - cap_net_raw
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: always
